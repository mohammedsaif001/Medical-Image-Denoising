import React from 'react'

function Convert() {
    // var options = {
    //     logLevel: 1
    // };

    // var converter = new ConvertTiff(options);
    // var location = ''

    // converter.convertOne('test.tif', location);
    return (
        <div>Fuck You Bitches</div>

    )
}

export default Convert